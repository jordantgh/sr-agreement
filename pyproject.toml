[project]
name = "sra-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.2.4",
    "scipy>=1.15.2",
]

[dependency-groups]
dev = [
  "pytest>=8.2.1",
  "pandas>=2.0.0",
]